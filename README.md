This project uses libraries that are not included in the vanilla version of R. To download these libraries, use the script "packages" and run it as it is. If you already have these libraries, then only run the part
that is calling them.
